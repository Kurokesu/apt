# Local aptly state and pipeline work dirs
/.aptly/
/publish/
/work/

# Fetched/extracted artifacts and checksums
*.deb
*.tar.gz
/SHA256SUMS
/SHA256SUMS.asc

# Python bytecode from running the manifest helper locally
__pycache__/

# Local-only dev overrides
*.local
